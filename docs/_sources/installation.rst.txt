.. _installation:

Installation
============

boto3-client-cache is available on PyPI.

To install with pip:

.. code-block:: bash

   pip install boto3-client-cache

To install with uv:

.. code-block:: bash

   uv pip install boto3-client-cache

To install with poetry:

.. code-block:: bash

   poetry add boto3-client-cache